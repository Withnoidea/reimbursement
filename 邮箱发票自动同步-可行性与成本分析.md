# 邮箱发票自动同步 · 可行性与成本分析

> 生成日期：2026-07-12
> 目标：在现有「报销单据管理」桌面应用里，点一下"同步"就能自动从邮箱拉取 / 下载 / 解析发票（含 ZIP 附件），复刻 QQ 邮箱发票系统的体验。

---

## 一、结论速览（TL;DR）

| 问题 | 结论 |
|---|---|
| **能不能免费实现？** | ✅ **能，软件层面 100% 可做到 0 元**。核心链路（收件 + 拉取 + 解析）全部有免费额度覆盖。 |
| **可行性高不高？** | ✅ **高**。你已经有完整的解析引擎，缺的只是"从邮箱取附件"这一段，工作量集中在 1 个模块。 |
| **最快能落地的方案** | **方案 C：应用直接用 IMAP 连 QQ 邮箱拉取**。0 外部配置，半天到一天打通。 |
| **最理想的最终形态** | **方案 A：免费域名 + Cloudflare 邮件转发 + IMAP 拉取**。对外只公布一个你自己的地址，后端随时可换。 |
| **不推荐的方案** | 方案 B：Cloudflare Worker + 云存储 + 自建 API。对个人报销场景属于过度工程。 |
| **唯一可能花钱的地方** | 如果不想用会被回收的免费域名，买个正式域名约 **¥10～70／年**（首年，`.top`/`.xyz` 很便宜）。 |
| **真正的成本** | 不是钱，是**开发工时**：MVP 约 1～2 天，生产可用约 3～5 天。 |

---

## 二、你现在已经有什么（资产盘点）

在谈"要加什么"之前，先明确"已经有什么"——这决定了工作量其实很小。

| 能力 | 现状 | 文件位置 |
|---|---|---|
| 桌面应用外壳 | Electron 37 + React 19 + Vite | `electron/main.js`、`src/App.jsx` |
| **PDF 发票解析（后端）** | PyMuPDF，提取金额/发票号/开票日期/页数 | `tools/parse_pdf.py` |
| **PDF 发票解析（前端）** | pdf.js，与后端同套正则逻辑 | `src/pdfParser.js` |
| 压缩包处理 | jszip（前端）+ yauzl（已随依赖存在） | `package.json`、`node_modules/yauzl` |
| 本地数据库 | IndexedDB（idb） | `src/storage.js` |
| 主进程↔渲染进程桥 | contextBridge / IPC | `electron/preload.js` |
| Python 子进程调用 | 主进程 spawn `.venv` 里的 python | `electron/main.js:57` |

**换句话说：** 一封发票邮件里的 PDF，只要能落到磁盘上一个路径，你现在的 `parsePdfWithPython(filePath)`（`electron/main.js:57`）就能直接解析出金额。你缺的**只是"把邮件附件下载到磁盘"这一步**。

---

## 三、核心澄清：三个关键误区

你在需求里提到几个想法，有的成立、有的不成立，先纠正，免得走弯路。

### 误区 1：「把邮箱挂在 GitHub 上」——❌ 不成立

GitHub Pages 是**静态网页托管**，不能接收邮件；GitHub Actions 能定时跑脚本，但**不能作为邮件服务器（MX）收信**。邮件后端这条路，GitHub 走不通。**Cloudflare 可以**（下面讲）。

### 误区 2：「Cloudflare 邮箱 = 一个能登录、能拉附件的网盘邮箱」——⚠️ 部分成立

Cloudflare 提供的是 **Email Routing（邮件路由 / 转发）**，不是带 IMAP 的邮箱：

- ✅ 它能让 `发票@你的域名.dpdns.org` 收信，**免费**，无限地址（catch-all）。
- ❌ 它**本身不提供 IMAP/POP3 收件箱**，你不能"登录 Cloudflare 邮箱"用客户端拉信。
- ✅ 它的工作方式是：**收到信后自动转发到你指定的、已验证的真实邮箱**（比如你的 QQ 邮箱 / Gmail）。
- ✅ 进阶：可以写 **Email Worker**（serverless 函数）拦截入站邮件，拿到原始 MIME、附件，转存到 R2/KV 或调你的 API——但这就是方案 B 的复杂度了。

**结论：** Cloudflare 负责"用你自己的域名收信并转发"，**真正被你的应用用 IMAP 拉取的，还是那个被转发到的真实邮箱（QQ/Gmail）**。域名邮箱是"门面 + 可迁移性"，不是拉取入口。

### 误区 3：「免费域名 dpdns.org 能用来做邮箱」——✅ 能，但有风险

`*.dpdns.org` 来自 DigitalPlat FreeDomain 这类免费二级域名服务：

- ✅ 允许把 nameserver 改到 Cloudflare，从而配 MX、启用 Email Routing。链路成立。
- ⚠️ **免费域名可能被回收 / 政策变动 / 不保证长期可用**，部分服务商条款里对"邮件用途"有限制（用前务必看它的 ToS）。
- ⚠️ 免费域名做发件容易进垃圾箱，但**你这里只用来"收"发票、不用来群发**，影响小。

**建议：** 免费域名适合验证链路、个人自用；若要长期稳定，花 ¥10～70 买个正式域名更省心。

---

## 四、三种方案对比

### 方案 C：应用直接 IMAP 拉 QQ 邮箱（最快落地）⭐ 起步首选

```
   供应商/开票方
        │ 把发票发到
        ▼
   你的 QQ 邮箱  ──IMAP(imap.qq.com:993)──►  你的桌面应用主进程
   (imap.qq.com)                                │ 下载 PDF/ZIP 附件到临时目录
                                                ▼
                                    现有 parse_pdf.py / pdfParser.js
                                                │
                                                ▼
                                    写入 IndexedDB（去重后入库）
```

- **要做什么**：在 Electron 主进程加一个 IMAP 客户端，连 QQ 邮箱（用**授权码**，非登录密码），拉未读/指定文件夹邮件，下载附件，交给现有解析器。
- **免费性**：QQ 邮箱 IMAP 免费。**总成本 ¥0**。
- **优点**：0 外部服务配置、最快、最稳、无域名依赖。
- **缺点**：对外公布的是 QQ 地址；换邮箱后端要改配置。
- **适合**：先打通"邮箱→解析"全链路，验证价值。

### 方案 A：免费域名 + Cloudflare 转发 + IMAP 拉取（推荐的最终形态）⭐⭐

```
   供应商/开票方
        │ 发到你的门面地址
        ▼
   发票@withnoidea.dpdns.org
        │ MX 指向 Cloudflare
        ▼
   Cloudflare Email Routing（免费转发）
        │ 自动转发到
        ▼
   你的 QQ 邮箱 / Gmail  ──IMAP──►  桌面应用 → 现有解析器 → 入库
```

- **要做什么**：① 注册免费域名 → ② nameserver 改到 Cloudflare → ③ 开 Email Routing，把 `*@你的域名` 转发到 QQ 邮箱 → ④ 应用侧仍走方案 C 的 IMAP 拉取（拉的是 QQ 邮箱，但里面都是转发进来的发票）。
- **免费性**：域名免费、Cloudflare 转发免费、IMAP 免费。**总成本 ¥0**。
- **优点**：对外只有一个你自己的、体面的地址；以后换邮箱/换服务商，只改 Cloudflare 转发目标，**对外地址不变**。
- **缺点**：多了域名 + Cloudflare 的一次性配置（约 30 分钟）；免费域名有回收风险。
- **适合**：链路跑通后，想要一个"专用、可迁移"的收票地址时升级。

> 关键点：方案 A 的应用侧代码和方案 C **完全一样**，只是邮箱里多了一批"从你域名转发进来的"邮件。所以 **A 是 C 的叠加升级，不是重写**。

### 方案 B：Cloudflare Email Worker + R2/KV + 自建 API（重，个人不推荐）

```
   发票@你的域名 ──► Cloudflare Email Worker（拦截入站邮件）
                          │ 解析 MIME、抽出附件
                          ├──► 存 R2 / KV（免费额度内）
                          │    并解析金额（Worker 里跑，或转发给外部服务）
                          ▼
   桌面应用 ──HTTP GET /invoices（带鉴权）──► 拉取已解析的发票清单
```

- **免费性**：Workers 免费版每天 10 万请求、R2 免费 10GB，个人用绰绰有余。**仍可 ¥0**。
- **优点**：云端预解析、多设备共享、不依赖某个真实邮箱的 IMAP。
- **缺点**：要写 Worker + 存储 + 鉴权 + HTTP 接口，还要在 Worker 里处理 PDF/OFD 解析（Worker 环境跑 PyMuPDF 不现实，得换 JS 解析或外部函数）。**复杂度和维护量陡增**。
- **适合**：将来要做多人/多端共享的"团队版"，个人单机用属于杀鸡用牛刀。

### 对比总表

| 维度 | 方案 C（直连 IMAP） | 方案 A（域名转发+IMAP） | 方案 B（Worker+API） |
|---|---|---|---|
| 金钱成本 | ¥0 | ¥0（免费域名）| ¥0（免费额度内）|
| 一次性配置 | 几乎没有 | 约 30 分钟 | 数小时 |
| 开发工时 | 1～2 天 | 1～2 天（同 C）| 4～7 天 |
| 对外地址 | QQ 地址 | 你自己的域名 ⭐ | 你自己的域名 |
| 可迁移性 | 低 | 高 ⭐ | 高 |
| 维护复杂度 | 低 ⭐ | 低 | 中～高 |
| 多端共享 | 否 | 否 | 是 ⭐ |
| **推荐度** | ⭐⭐⭐ 先做这个 | ⭐⭐⭐ 最终形态 | ⭐ 暂不需要 |

---

## 五、推荐路线

> **先 C，后 A，暂不碰 B。**

1. **第一步（打通价值）**：做**方案 C**——应用直接 IMAP 拉 QQ 邮箱。把"点同步 → 自动下载附件 → 解析金额 → 入库去重"整条链路跑通。这一步就已经复刻了 QQ 邮箱发票系统的核心体验。
2. **第二步（升级门面）**：等链路稳定，再叠加**方案 A**——注册免费域名、Cloudflare 转发到 QQ 邮箱。**应用代码几乎不用改**，只是有了一个专属收票地址。
3. **暂缓（除非要多人共享）**：方案 B 留到有"团队 / 多设备实时共享"需求时再说。

---

## 六、落地步骤（结合你现有代码）

以下针对**方案 C（也是 A 的应用侧）**，给出具体改造点。

### 6.1 主进程加 IMAP 拉取（`electron/main.js`）

新增一个 IPC handler，用 [`imapflow`](https://github.com/postalsys/imapflow)（现代、Promise 化）连接邮箱、下载附件到临时目录：

```js
// 伪代码示意，非最终实现
import { ImapFlow } from "imapflow";
import { simpleParser } from "mailparser";

ipcMain.handle("sync-mailbox", async (_e, config) => {
  const client = new ImapFlow({
    host: "imap.qq.com", port: 993, secure: true,
    auth: { user: config.user, pass: config.authCode }, // 授权码，非密码
  });
  await client.connect();
  const lock = await client.getMailboxLock("INBOX");
  const saved = [];
  try {
    // 只拉未处理过的（按 UID 增量，见 6.4 去重）
    for await (const msg of client.fetch({ uid: `${config.lastUid + 1}:*` }, { source: true })) {
      const mail = await simpleParser(msg.source);
      for (const att of mail.attachments) {
        // 落盘到临时目录，PDF 直接解析，ZIP 先解压再逐个解析
        saved.push(await handleAttachment(att)); // 见 6.3
      }
    }
  } finally {
    lock.release();
    await client.logout();
  }
  return saved; // 返回 { filePath, amount, invoiceNo, invoiceDate } 列表
});
```

- 附件落盘后，PDF 直接复用现有 `parsePdfWithPython(filePath)`（`electron/main.js:57`），**解析逻辑一行都不用改**。

### 6.2 preload 暴露接口（`electron/preload.js`）

```js
// 在现有 desktopApi 里加一个
syncMailbox(config) {
  return ipcRenderer.invoke("sync-mailbox", config);
},
```

### 6.3 附件处理：PDF 直接解析，ZIP 先解压

- **PDF**：落盘 → `parsePdfWithPython(path)`。
- **ZIP**：主进程用 `yauzl`（已在依赖里）或 `adm-zip` 解压，取出里面的 `.pdf`（以及可能的 `.ofd`，见风险 8.5），逐个走解析。
- 你前端已有 jszip 处理压缩包的经验，这套逻辑可平移到主进程。

### 6.4 去重（重要，避免重复导入）

现有 `addDocument`（`src/storage.js:90`）是无条件 `add`。同步会反复拉到同一封信，必须去重：

- **按邮件 UID 增量**：记录每个邮箱上次同步到的最大 UID，下次只拉更大的。
- **按发票号码去重**：`invoiceNo` 是发票天然唯一键；入库前查一下是否已存在。建议给 `documents` 加一个 `invoiceNo` 索引。
- 两层一起用最稳。

### 6.5 UI：同步按钮 + 邮箱配置

- `src/App.jsx` 顶部工具栏加一个「同步邮箱」按钮，调用 `window.desktopApi.syncMailbox(config)`，复用现有 `busy` / `toast` 做进度和结果提示。
- 「偏好设置」弹窗（已有 `openPreferences`，`src/App.jsx:117`）里加一组表单：IMAP 服务器 / 端口 / 账号 / 授权码 / 要同步的文件夹。

### 6.6 凭据安全存储（别明文存授权码）

用 Electron 的 [`safeStorage`](https://www.electronjs.org/docs/latest/api/safe-storage) 把授权码加密后再落盘（它走系统级密钥：Windows DPAPI / macOS Keychain）。**不要**把授权码明文写进 IndexedDB 或配置文件。

---

## 七、成本明细

### 7.1 直接金钱成本

| 项目 | 方案 C | 方案 A | 方案 B | 说明 |
|---|---|---|---|---|
| 邮箱（QQ/Gmail）IMAP | ¥0 | ¥0 | ¥0 | 开授权码即可 |
| 免费域名 `*.dpdns.org` | — | ¥0 | ¥0 | 可选，有回收风险 |
| Cloudflare Email Routing | — | ¥0 | ¥0 | 免费转发 |
| Cloudflare Workers / R2 / KV | — | — | ¥0 | 免费额度内 |
| 桌面应用改造 | ¥0 | ¥0 | ¥0 | 自己写 |
| **合计** | **¥0** | **¥0** | **¥0** | — |

> 可选升级：正式域名约 **¥10～70／年**（首年，`.top`/`.xyz`/`.com` 视后缀而定），用于替代免费域名求稳定。

### 7.2 开发工时成本（真正的成本）

| 阶段 | 内容 | 预估 |
|---|---|---|
| MVP | IMAP 连接 + 下载附件 + 复用解析 + 简单入库 | **1～2 天** |
| 生产可用 | 去重、增量同步、错误重试、凭据加密、进度反馈、配置 UI | **再 2～3 天** |
| 方案 A 叠加 | 注册域名 + Cloudflare 转发配置 | **约 0.5 天** |
| 可选：OFD / OCR 支持 | 见风险 8.5 / 8.6 | **各 1～2 天** |

---

## 八、技术风险与坑（务必先看）

### 8.1 免费域名回收风险 ⚠️
`*.dpdns.org` 这类免费域名可能因政策变动被收回。**别把它当长期唯一入口**；重要场景用正式域名。

### 8.2 IMAP 授权码安全 ⚠️
授权码等价于邮箱访问权，**必须用 safeStorage 加密存**，不能明文。

### 8.3 邮箱 IMAP 限流 ⚠️
QQ 邮箱对频繁连接有限流。同步做成"手动点击 + 合理间隔"，加重试与退避，别做高频轮询。

### 8.4 Cloudflare 转发的邮件大小上限 ⚠️
Email Routing 单封邮件有大小上限（约 25MB／封，**以官方文档为准**）。发票 PDF/ZIP 通常几百 KB～几 MB，安全；但如果一封信塞很多大附件超限，会被拒收。

### 8.5 OFD 格式发票 ❗（现有解析器不支持）
全电（数电）发票有 **PDF 和 OFD 两种格式**。你现在的 `parse_pdf.py` 只吃 PDF。OFD 本质是 ZIP 包着 XML，需要单独解析器（Python 的 `easyofd`，或自己解压读 XML）。**如果你的发票里有 OFD，要单独加一个解析分支。**

### 8.6 图片型 / 扫描件发票 ❗（无文本层解析不到）
现有解析是**读 PDF 文本层**。如果是扫描件或纯图片发票（无文本层），提取不到金额，需要 OCR（本地可用 PaddleOCR/Tesseract，免费但要额外集成）。**好消息**：正规数电发票都带文本层，一般不受影响。

### 8.7 金额提取的边界情况
现有 `extract_amount`（`tools/parse_pdf.py:43`）取"小写金额"或页面最大货币数字。绝大多数增值税发票 OK，但遇到含多个大额数字的复杂版式（如带清单的发票）可能误取，建议同步后保留人工校对入口（你 UI 里已有编辑能力）。

### 8.8 附件命名与编码
邮件附件名可能是中文 / RFC2047 编码 / 重名。落盘时要解码文件名、加唯一前缀，避免覆盖。`mailparser` 会帮你处理大部分编码。

---

## 九、开发任务分解（可直接照做）

- [ ] **依赖**：`npm i imapflow mailparser`；ZIP 用已有 `yauzl` 或 `npm i adm-zip`。
- [ ] **主进程**：新增 `sync-mailbox` IPC handler（连接 → 增量 fetch → 附件落盘 → 解析）。
- [ ] **附件分流**：PDF→现有解析；ZIP→解压后逐个解析；OFD→（可选）新增分支。
- [ ] **去重**：`documents` 加 `invoiceNo` 索引；记录各邮箱 `lastUid` 做增量。
- [ ] **凭据**：safeStorage 加密存 IMAP 授权码。
- [ ] **preload**：暴露 `syncMailbox`。
- [ ] **UI**：工具栏「同步邮箱」按钮 + 偏好设置里的 IMAP 配置表单 + 进度/结果提示。
- [ ] **（升级）方案 A**：注册免费域名 → NS 改到 Cloudflare → 开 Email Routing 转发到 QQ 邮箱。

### 常用参数速查

| 邮箱 | IMAP 服务器 | 端口 | 认证 |
|---|---|---|---|
| QQ 邮箱 | `imap.qq.com` | 993 (SSL) | 设置里开启 IMAP，生成**授权码** |
| Gmail | `imap.gmail.com` | 993 (SSL) | 开两步验证后生成**应用专用密码** |
| 163 邮箱 | `imap.163.com` | 993 (SSL) | 开启 IMAP，生成**授权码** |

---

## 十、需要你自己确认的事项

1. **免费域名的服务条款**：用前确认 `dpdns.org` 提供方是否允许"邮件/MX"用途，以及回收政策。
2. **Cloudflare 具体免费额度与邮件大小上限**：以 Cloudflare 官方文档现值为准（本文档写作时约 25MB/封）。
3. **你的发票格式构成**：主要是 PDF 还是也有 OFD？有没有图片/扫描件？这决定要不要做 8.5 / 8.6 两项额外工作。
4. **收票邮箱选谁**：QQ / Gmail / 163——建议用一个**专用邮箱**只收发票，和私人邮件隔离，同步更干净。

---

### 一句话总结

> **钱：0 元就能做（方案 C/A 全免费）。难点不在钱，在开发——而你 80% 的活（解析引擎）已经做完了，剩下的只是接一根"邮箱进料管"。建议先用 IMAP 直连 QQ 邮箱打通链路（1～2 天），再按需叠加免费域名做专属地址。GitHub 收不了邮件、Cloudflare 是转发不是网盘邮箱——这两点别踩坑。**
